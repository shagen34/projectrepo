/*
Shane Hagen
EE 456
Homework Assignment - Lateral Inhibition
*/

import java.io.*;
import java.util.*;
import java.math.*;
import java.text.*;

public class Solution {

  public static void main(String[] args) {
    double r1 = 2 //Radius of excitation
    double r2 = 4 //Radius of Inhibition

    double[7] intial = [0, 0, 1, 1, 1, 0, 0]
    double[10] weights = [-0.2, -0.4, 0.2, 0.4, 1.0, 0.4, 0.2, -0.4, -0.2];

    int count= 0;
    while(count < intial.legth){
      double newx = 0;

      while(){
        if(useRadius-r2 < 0){
          newx += 0
        }
        else{
          newx += intial[count] + weights[useRadius-r2];
        }
      }


























      while(int )
      for(int pos = 0; pos<r2; pos++){
        double newx = 0;
        if(pos < r1)
          if(pos-r1 < 0){
            newx+=0;
          }
          else{
            newx +=
          }
        }
      }
    }
  }
};
